<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Application Base Theme Colors -->
    <color name="theme_primary">@color/primary</color>
    <color name="theme_primary_dark">@color/primary_dark</color>
    <color name="theme_primary_light">@color/primary_light</color>
    <color name="theme_secondary">@color/secondary</color>
    <color name="theme_secondary_dark">@color/secondary_dark</color>
    <color name="theme_secondary_light">@color/secondary_light</color>

    <color name="colorPrimary">@color/theme_primary</color>
    <color name="colorPrimaryDark">@color/theme_primary_dark</color>
    <color name="colorAccent">@color/accent_color</color>

    <!-- Drawer Properties -->
    <color name="drawerDefaultBackgroundColor">#ffffff</color>
    <dimen name="drawerDefaultWidth">280dp</dimen>
    <dimen name="drawerAccountBottomPadding">16dp</dimen>
    <dimen name="drawerAvatarWidthHeight">60dp</dimen>

    <!-- Dimensions -->
    <dimen name="statusBarHeight">0dp</dimen>

    <!-- Booleans -->
    <bool name="fitToScreenWindow">true</bool>
</resources>
